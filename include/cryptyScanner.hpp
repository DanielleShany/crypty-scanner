#pragma once

#include "MagicIdentifier.hpp"
#include "SignatureScanner.hpp"

int run(const std::string& root_dir, const std::string& sig_path);
int invalidInput(const string& root_dir, const string& sig_path);

